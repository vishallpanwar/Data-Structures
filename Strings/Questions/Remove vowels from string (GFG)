class Solution{
public:	
		
	string removeVowels(string S) 
	{
	    vector<int>v;
	    for(int i=0;i<S.size();i++)
	    {
	        v.push_back(S[i]);
	        if(S[i]=='a' || S[i]=='e' || S[i]=='i' || S[i]=='o' || S[i]=='u')
	            continue;
	        else
	            v.push_back(S[i]);
	    }
	    return v;
	}
};
