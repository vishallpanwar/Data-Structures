#include <iostream>
#include<string.h>
using namespace std;

void convUpperCase(char arr[]){

    int n=strlen(arr);

    for(int i=0;i<n;i++){

        arr[i]=arr[i]-'a'+'A';
    }
}

int main(){

    char arr[100]="vishal";
    convUpperCase(arr);
    cout<<arr;
    return 0;
}
