class Solution {
  public:
    string toLower(string S) {
        
        for(int i=0 ; i< S.size() ; i++){
            if(S[i] >= 'A' && S[i] <= 'Z'){
                S[i]= tolower(S[i]);
            }
        }
        return S;
    }
};
