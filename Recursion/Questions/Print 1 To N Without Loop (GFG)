class Solution{
    public:
    
    void printNos(int N)
    {
        // Base case
        if(N==0)
        return;
        
        // Recursive call
        printNos(N-1);
        cout<<N<<" ";
    }
};
