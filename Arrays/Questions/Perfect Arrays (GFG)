class Solution{
    public:
    bool IsPerfect(int a[],int n)
    {
        int x=n;
        
        for(int i=0;i<n;i++){
            x--;
            
        if(a[i] != a[x])
        return false;
        
        }

        return true;
    }
};
